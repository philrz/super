script: |
  ! super db -db=path/to/superdb ls

outputs:
  - name: stderr
    regexp: |
      .* database does not exist
      \(hint: run 'super db init' to initialize a database at this location\)
  - name: stdout
    data: ""
