script: |
  ! super db ls -db ''
  ! super db serve -db ''

outputs:
  - name: stderr
    data: |
      database location must be set (either with the -db flag or SUPER_DB environment variable)
      database location must be set (either with the -db flag or SUPER_DB environment variable)
