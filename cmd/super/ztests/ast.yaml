script: |
  super -C -c "?a"
  super -C -I query.spq
  super -C -I query.spq -c a

inputs:
  - name: query.spq
    data: |
      ? q

outputs:
  - name: stdout
    data: |
      search a
      search q
      search q and a
