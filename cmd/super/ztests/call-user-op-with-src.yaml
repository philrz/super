script: |
  super -s -I countfile.spq -c countfile

inputs:
  - name: countfile.spq
    data: |
      op countfile: (
        from test.sup | count()
      )
  - name: test.sup
    data: '{} {} {} {}'

outputs:
  - name: stdout
    data: |
      4::uint64
