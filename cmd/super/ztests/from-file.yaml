script: |
  super -s -c 'from a.sup'
  super -s -I query.spq

inputs:
  - name: a.sup
    data: |
      {a:1}
  - name: query.spq
    data: |
      from a.sup

outputs:
  - name: stdout
    data: |
      {a:1}
      {a:1}
