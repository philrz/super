# This was previously getting parsed as an 'in' comparator. Test that we are no
# longer doing this
spq: '? x==1 inaction'

vector: true

input: |
  {x:1,text:"inaction"}

output: |
  {x:1,text:"inaction"}
