script: |
  ! super compile -dag 'select * from /dev/null with ordinality'

outputs:
  - name: stderr
    data: |
      WITH ORDINALITY clause is not yet supported at line 1, column 24:
      select * from /dev/null with ordinality
                             ~~~~~~~~~~~~~~~~
