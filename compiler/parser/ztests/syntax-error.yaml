script: |
  ! super -z -c 'count() by ,x,y' -

inputs:
  - name: stdin
    data: ""

outputs:
  - name: stderr
    data: |
      super: parse error at line 1, column 12:
      count() by ,x,y
             === ^ ===
