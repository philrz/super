script: |
  ! super -s -c 'count() by ,x,y' -

inputs:
  - name: stdin
    data: ""

outputs:
  - name: stderr
    data: |
      parse error at line 1, column 12:
      count() by ,x,y
             === ^ ===
