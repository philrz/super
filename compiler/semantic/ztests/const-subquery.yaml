spq: |
  op addEm: (sum(this))
  fn foo(a) : (
    unnest a | addEm
  )
  const bar = [
    values {a:[1,2,3],b:[10]}, {a:[4,5],b:[11,23]}
    | {a:foo(a),b:foo(b)}
  ]
  values bar,bar

output: |
  [{a:6,b:10},{a:9,b:34}]
  [{a:6,b:10},{a:9,b:34}]
