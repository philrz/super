spq: |
  select a as b
  from (values (0,4),(1,3),(2,2)) T(a,b)
  group by a,b
  having b >= 1
  order by b

output: |
  {b:1}
  {b:2}

---

spq: |
  select a
  from (values (0,4),(1,3),(2,2)) T(a,b)
  group by a,b
  having b >= 1
  order by T.b

output: |
  {a:2}
  {a:1}
  {a:0}
