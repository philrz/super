script: |
  super compile -C "? is(this, <foo>) bar"
  super compile -dag -C "? is(this, <uint16|ip>) 80"

outputs:
  - name: stdout
    data: |
      search is(this, <foo>) and bar
      null
      | where is(this, <uint16|ip>) and search(80)
      | output main
