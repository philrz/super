spq: |
  pass

input: |
  "HEADS"::(flip=(enum(HEADS,TAILS)))
  "TAILS"::flip
  "HEADS"::flip

output: |
  "HEADS"::(flip=enum(HEADS,TAILS))
  "TAILS"::(flip=enum(HEADS,TAILS))
  "HEADS"::(flip=enum(HEADS,TAILS))
