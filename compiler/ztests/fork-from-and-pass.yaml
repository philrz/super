skip: XXX let's discuss this one

script: |
  super -c 'fork (=> from (file a.sup) => pass)' a.sup

inputs:
  - name: a.sup
    data: |
      {a:1}

outputs:
  - name: stdout
    data: |
      {a:1}
      {a:1}
