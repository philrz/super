spq: |
  WITH t AS (
    SELECT 1 x
      UNION ALL
    SELECT x+1 x
    FROM   t
    WHERE  x < 200000
  )
  SELECT count(x) FROM t AS t1;

input: null

error: |
  recursive WITH relations not currently supported at line 5, column 10:
    FROM   t
           ~
