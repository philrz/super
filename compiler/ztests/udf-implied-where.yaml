script: |
  super compile -C -dag 'fn h(e): ( has(e) ) h(foo)'

outputs:
  - name: stdout
    data: |
      null
      | (
        fn h(e): (
          has(e)
        )
        
        where h(foo)
        | output main
      )
