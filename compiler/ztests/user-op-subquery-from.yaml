script: |
  super -s -I query.spq

inputs:
  - name: query.spq
    data: |
      let slurp = (from in.sup | count())
      values {x:slurp}, {y:slurp}
  - name: in.sup
    data: |
      1
      2
      3

outputs:
  - name: stdout
    data: |
      {x:3::uint64}
      {y:3::uint64}
