script: |
  export SUPER_DB=test
  super db init -q
  super db create -q test
  mv test/superdb.bsup superdb-orig.bsup
  super -o test/superdb.bsup -c 'version:=1' superdb-orig.bsup
  ! super db serve

outputs:
  - name: stderr
    data: |
      unsupported database version: found version 1 while expecting 5
