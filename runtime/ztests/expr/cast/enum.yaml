spq: this::enum(e1,e2)

vector: true

input: |
  "e2"
  "e3"
  0
  null
  null::string
  null::int64

output: |
  "e2"::enum(e1,e2)
  error({message:"no such symbol in enum(e1,e2)",on:"e3"})
  error({message:"cannot cast to enum",on:0})
  null::enum(e1,e2)
  null::enum(e1,e2)
  null::enum(e1,e2)
