spq: yield a==b, a!=b, a<b, a<=b, a>=b, a>b

vector: true

input: |
  {a:192.168.0.0/23,b:192.168.1.0/23}

output: |
  true
  false
  error("type net incompatible with '<' operator")
  error("type net incompatible with '<=' operator")
  error("type net incompatible with '>=' operator")
  error("type net incompatible with '>' operator")
