script: |
  ! super -z -c 'join on a=b' A.jsup

inputs:
  - name: A.jsup
    data: |
      {a:1(int32)}
      {a:2(int32)}
      {a:3(int32)}

outputs:
  - name: stderr
    data: |
      join requires two upstream parallel query paths
