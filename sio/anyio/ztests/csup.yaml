script: |
  super -f csup -o f -
  super -s f
  echo // standard input
  super -s -c 'from "stdio:stdin"' < f

inputs:
  - name: stdin
    data: |
      {a:1}

outputs:
  - name: stdout
    data: |
      {a:1}
      // standard input
      {a:1}
