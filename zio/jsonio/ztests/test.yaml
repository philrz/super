script: super -i json -f zng - | super -f json -

inputs:
  - name: stdin
    data: |
      {"a": {"b": "1"}}

outputs:
  - name: stdout
    data: |
      {
          "a": {
              "b": "1"
          }
      }
