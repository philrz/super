zed: "*"

output-flags: -f jsup -pretty=4

input: |
  {flip:%HEADS(enum(HEADS,TAILS))}
  {flip:%TAILS(enum(HEADS,TAILS))}
  {flip:%HEADS(enum(HEADS,TAILS))}

output: |
  {
      flip: %HEADS (enum(HEADS,TAILS))
  }
  {
      flip: %TAILS (enum(HEADS,TAILS))
  }
  {
      flip: %HEADS (enum(HEADS,TAILS))
  }
