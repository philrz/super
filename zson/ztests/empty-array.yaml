script: |
  super -z in.sup

inputs:
  - name: in.sup
    data: |
      []
      [1]
      {version:[]([null])}
      {version:[1]}

outputs:
  - name: stdout
    data: |
      []
      [1]
      {version:[]([null])}
      {version:[1]}
