script: |
  super -z in.jsup

inputs:
  - name: in.jsup
    data: |
      []
      [1]
      {version:[]([null])}
      {version:[1]}

outputs:
  - name: stdout
    data: |
      []
      [1]
      {version:[]([null])}
      {version:[1]}
